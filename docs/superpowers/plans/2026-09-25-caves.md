# Delprojekt 3 – Hål och grottor: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Varje värld får en synlig grotta (hål i marken) och en eller två dolda (buske som skjuts bort, sten med spricka, öppning i en klippvägg). Grottorna är handgjorda bonusbanor – skattkammare eller mörkergrotta – med en ljusstråle som leder tillbaka till ytan.

**Architecture:** `src/terrain.js` får ett grottläge (icke-loopande mark med bredd och tak), hål som kan öppnas/stängas i spel, samt spara/återställ av hela terrängtillståndet. Ny modul `src/caves.js` bygger ingångsobjekt för ytan, öppnar dolda ingångar, byter scen (`G.scene` = `'surface' | 'cave'`) med snapshot av ytan, hanterar skatter och utgång. `game.js`/`player.js` kopplar in det (ingen fiendespawn i grotta, kamera klämd till grottan, tak och väggar). `render.js` ritar grotta, hål, ingångar, skatter och mörker.

**Tech Stack:** Vanilla JS ES-moduler, JSON-banfiler, Canvas 2D, Vitest.

**Spec:** `docs/superpowers/specs/2026-09-25-world-variation-design.md` (avsnitt "Delprojekt 3 – Hål och grottor")

## Global Constraints

- Grottor är ren bonus: de ger gröna pärlor (`gem`), poängstjärnor (`star`, +200) och högst ett extraliv (`life`) per värld – aldrig färgdroppar.
- Per värld: exakt en `hole`-grotta (synlig) + 1–2 dolda (`bush`, `rock`, `cliff`).
- Ingångar: `hole` (hål, bredd 70), `bush` (ej solid, 1 träff → hål), `rock` (solid sten w 64 h 46 med spricka, 3 träffar → hål), `cliff` (solid vägg w 40 h 300, öppning på höjd `entrance.y`; in genom att flyga in i öppningen ±26).
- Grottdata: `{ id, entrance: { kind, x, w?, y? }, type: 'treasure'|'dark', width, floor[width/100+1], ceiling[width/100+1], objects?, loot[], foes? }`. Alla vertikala värden = höjd över skärmens botten.
- Grottor loopar inte; kameran kläms till `[0, width − W]`. Bollen stoppas av tak och ändväggar. Ljusstråle/utgång när `P.x > width − 60`.
- Ingång räknas vid inträde: `G.cavesUsed` (Set med grott-id). Efter utgång stängs ingången för resten av världen. "Secrets found: X/Y" = `G.cavesUsed.size` / antal grottor i världen, visas när världen är klar.
- Ytan sparas vid inträde (terräng inkl. objektstatus, fiender, skott, droppar, pärlor, glöd, kamera, spawn-timer) och återställs exakt vid utgång.
- Mörkergrotta: allt utom en ljuscirkel (~130) runt bollen är svart; färglösa svävande fiender (`foes`) som inte försvinner p.g.a. avstånd.
- Menyns bakgrundsdemo har inga ingångar.
- UI-text engelska, kod engelska, inga runtime-beroenden.

## Review Focus

1. **Ytan efter ett grottbesök** – förstörda kristaller, fiender, droppar, kamera och glöd ska vara exakt som innan. Test: `test/caves.test.js` "leaves the surface exactly as it was" (Task 3).
2. **Fall ner i ett hål utan grotta** (t.ex. en ingång som redan stängts eller datafel) – bollen får aldrig falla för evigt. Test: `test/caves.test.js` "returns the ball to a closed entrance" (Task 3).
3. **Varje grotta går att ta sig igenom utan Thrust** – ingen trappa/takhöjd som stänger vägen. Test: `test/playability.test.js` "cave %s can be crossed in spin mode" (Task 3).
4. **Saker som faller ner i hål** (droppar, hoppfiender) – ska försvinna, inte ligga kvar under skärmen och räknas. Test: `test/caves.test.js` "removes drops that fall into a hole" (Task 3).
5. **Extraliv** – högst ett per värld, även om spelaren går in i flera grottor. Test: `test/caves.test.js` "hides the extra life once it has been taken" (Task 3).

---

## Filstruktur

| Fil | Ändring |
|---|---|
| `src/terrain.js` | Skrivs om: tillstånd i ett objekt `T`; grottläge (`width`, `ceiling`), `ceilingAt`, `caveWidth`, `holes`, `holeAt`, `openHole`, `closeHole`, `saveTerrain`, `restoreTerrain`; nya former `bush`, `cliff` |
| `src/levels/*.json` | `caves` per värld |
| `src/caves.js` | Ny: `caveById`, `surfaceLevel`, `breakEntrance`, `enterCave`, `exitCave`, `tryCliff`, `updateCaves` |
| `src/state.js` | `scene`, `cave`, `surfaceSnapshot`, `cavesUsed`, `lifeTaken`, `loot` |
| `src/game.js` | `startLevel` nollställer grottor och laddar ytan med ingångar; ingen spawn i grotta; ingångar går sönder av skott; filtrerar fiender under skärmen; kamera kläms; `updateCaves` |
| `src/player.js` | Tak/väggar via `ceilingAt`/`caveWidth`; `tryCliff` |
| `src/pickups.js` | `collectGem` utbruten; droppar under skärmen tas bort |
| `src/render.js` | `drawCave`, hål, `bush`/`cliff`/sprucken sten, `drawLoot`, mörker |
| `src/hud.js` | "Secrets found" vid klar värld |
| `test/terrain.test.js`, `test/levels.test.js`, `test/player.test.js`, `test/playability.test.js` | Utökas |
| `test/caves.test.js` | Ny |

---

### Task 1: Terräng med grottläge, hål i spel och spara/återställ

**Files:**
- Modify: `src/terrain.js` (hela filen)
- Test: `test/terrain.test.js`

**Interfaces:**
- Consumes: `L`, `TAU`, `TOP` (config), `view` (state), `mod`, `wd`, `clamp` (util)
- Produces (utöver befintliga `defaultGround`, `loadTerrain`, `heightAt`, `groundAt`, `objects`, `objBox`, `surfaceBelow`, `collideCircle`, `touchesHazard`, `hazardBelow`, `hitObjectWithBullet`, `damageObject`, `updateObjects`):
  - `loadTerrain(level)` – med `level.width` laddas en grotta: `ground` = golv (`width/100+1` punkter, ingen loop), `ceiling` = tak
  - `ceilingAt(x): number` – skärm-y för taket; `TOP` på ytan
  - `caveWidth(): number | null` – grottans bredd, `null` på ytan
  - `holes(): object[]`, `holeAt(x): hole | null`
  - `openHole(x, w, cave)`, `closeHole(cave)`
  - `saveTerrain(): object`, `restoreTerrain(state)`

- [ ] **Step 1: Lägg till testerna i `test/terrain.test.js`**

Ändra importraderna högst upp till:

```js
import { describe, it, expect, beforeEach } from 'vitest';
import { loadTerrain, groundAt, surfaceBelow, collideCircle, touchesHazard, hazardBelow, hitObjectWithBullet, damageObject, objects, ceilingAt, caveWidth, openHole, closeHole, holeAt, saveTerrain, restoreTerrain } from '../src/terrain.js';
import { view } from '../src/state.js';
import { L, TAU, TOP } from '../src/config.js';
```

och lägg till sist i filen:

```js
describe('cave terrain', () => {
  const cave = { width: 1200, ground: [70, 75, 90, 70, 60, 80, 110, 140, 120, 90, 70, 70, 70], ceiling: [300, 320, 340, 330, 300, 290, 320, 360, 380, 360, 330, 310, 300] };
  beforeEach(() => { view.H = 540; loadTerrain(cave); });

  it('does not loop and clamps at the cave ends', () => {
    expect(groundAt(0)).toBe(540 - 70);
    expect(groundAt(700)).toBe(540 - 140);
    expect(groundAt(1200)).toBe(540 - 70);
    expect(groundAt(1300)).toBe(540 - 70);
    expect(caveWidth()).toBe(1200);
  });
  it('has a ceiling', () => {
    expect(ceilingAt(800)).toBe(540 - 380);
  });
  it('uses TOP as ceiling and has no width on the surface', () => {
    loadTerrain({});
    expect(ceilingAt(800)).toBe(TOP);
    expect(caveWidth()).toBeNull();
  });
});

describe('holes at runtime', () => {
  beforeEach(() => { view.H = 540; loadTerrain({ ground: Array(48).fill(100) }); });

  it('opens and closes a hole tagged with a cave', () => {
    openHole(1000, 70, 'c1');
    expect(groundAt(1000)).toBeNull();
    expect(holeAt(1020)).toMatchObject({ cave: 'c1' });
    closeHole('c1');
    expect(groundAt(1000)).toBe(440);
    expect(holeAt(1020)).toBeNull();
  });
  it('saves and restores terrain including object state', () => {
    loadTerrain({ ground: Array(48).fill(100), objects: [{ type: 'crystal', x: 500, c: 0 }] });
    objects()[0].gone = true;
    const saved = saveTerrain();
    loadTerrain({ width: 1200, ground: Array(13).fill(60), ceiling: Array(13).fill(300) });
    expect(caveWidth()).toBe(1200);
    restoreTerrain(saved);
    expect(caveWidth()).toBeNull();
    expect(objects()[0].gone).toBe(true);
    expect(groundAt(1000)).toBe(440);
  });
});
```

- [ ] **Step 2: Kör och se dem fallera**

Run: `npm test 2>&1 | grep -E "FAIL|Tests"`
Expected: FAIL – `ceilingAt`/`caveWidth`/`openHole` m.fl. finns inte.

- [ ] **Step 3: Skriv om `src/terrain.js`**

```js
import { L, TAU, TOP } from './config.js';
import { view } from './state.js';
import { mod, wd, clamp } from './util.js';

const SHAPES = {
  rock:     { solid: true },
  pillar:   { solid: true, w: 28, stopsShots: true },
  crystal:  { solid: true, w: 26, h: 36, stopsShots: true, hp: 3 },
  mushroom: { solid: true, w: 44, h: 28, bounce: 1.6 },
  cloud:    { h: 14 },
  thorns:   { h: 14, hazard: true },
  bush:     { w: 50, h: 34 },
  cliff:    { solid: true, w: 40, h: 300, stopsShots: true },
  hole:     {},
};

let T = {};

export function defaultGround(n = 240) {
  return Array.from({ length: n }, (_, i) => { const t = i / n * TAU; return 82 + 22 * Math.sin(t * 5) + 12 * Math.sin(t * 13 + 1.3) + 6 * Math.sin(t * 31 + .4); });
}

export function loadTerrain(level) {
  const cave = level.width != null, pts = Array.isArray(level.ground) ? level.ground : defaultGround();
  const all = (level.objects || []).map(o => ({ ...SHAPES[o.type], ...o, flash: 0, squash: 0, gone: false }));
  T = {
    pts, ceil: cave ? level.ceiling : null, loop: !cave, width: cave ? level.width : L,
    step: cave ? level.width / (pts.length - 1) : L / pts.length,
    holes: all.filter(o => o.type === 'hole'), objs: all.filter(o => o.type !== 'hole'),
  };
}

export const saveTerrain = () => T;
export function restoreTerrain(state) { T = state; }

function sample(arr, x) {
  const n = arr.length, u = T.loop ? mod(x, L) / T.step : clamp(x, 0, T.width) / T.step;
  const i = Math.min(Math.floor(u), T.loop ? n - 1 : n - 2), f = u - i;
  const a = arr[i % n], b = arr[(i + 1) % n];
  return a + (b - a) * (1 - Math.cos(Math.PI * f)) / 2;
}

export const heightAt = x => sample(T.pts, x);
export const ceilingAt = x => T.ceil ? view.H - sample(T.ceil, x) : TOP;
export const caveWidth = () => T.loop ? null : T.width;

export const holes = () => T.holes;
export function holeAt(x) { return T.holes.find(h => Math.abs(wd(x - h.x)) < h.w / 2) || null; }
export function openHole(x, w, cave) { T.holes.push({ type: 'hole', x, w, cave }); }
export function closeHole(cave) { T.holes = T.holes.filter(h => h.cave !== cave); }

export function groundAt(x) { return holeAt(x) ? null : view.H - heightAt(x); }

export const objects = () => T.objs;

export function objBox(o) {
  if (o.y != null) { const top = view.H - o.y; return { top, bottom: top + o.h }; }
  const base = groundAt(o.x) ?? view.H;
  return { top: base - o.h, bottom: base + 60 };
}

export function surfaceBelow(x, y) {
  let best = null;
  for (const o of T.objs) {
    if (o.gone || !(o.solid || o.type === 'cloud')) continue;
    if (Math.abs(wd(x - o.x)) > o.w / 2) continue;
    const top = objBox(o).top;
    if (top >= y - 1 && (!best || top < best.y)) best = { y: top, kind: o.type, obj: o };
  }
  if (best) return best;
  const g = groundAt(x);
  return g == null ? null : { y: g, kind: 'ground', obj: null };
}

export function collideCircle(b, prevBottom = Infinity) {
  let hit = null;
  for (const o of T.objs) {
    if (o.gone || !o.solid) continue;
    const box = objBox(o), dx = wd(b.x - o.x), hw = o.w / 2;
    if (box.top >= prevBottom - 1) continue; // was above the top last frame: landing handles it
    const ex = dx - Math.max(-hw, Math.min(hw, dx)), ey = b.y - Math.max(box.top, Math.min(box.bottom, b.y));
    if (ex * ex + ey * ey >= b.r * b.r) continue;
    if (ey < 0 && -ey >= Math.abs(ex)) continue; // contact from above: landing handles it
    const side = dx < 0 ? -1 : 1;
    b.x += side * (hw + b.r) - dx;
    if (b.vx * side < 0) b.vx = -b.vx * .6;
    hit = o;
  }
  return hit;
}

export function touchesHazard(b) {
  for (const o of T.objs) {
    if (o.gone || !o.hazard) continue;
    if (Math.abs(wd(b.x - o.x)) < o.w / 2 + b.r * .5 && b.y + b.r > objBox(o).top + 3) return o;
  }
  return null;
}

export function hazardBelow(x, r) {
  return T.objs.some(o => !o.gone && o.hazard && Math.abs(wd(x - o.x)) < o.w / 2 + r);
}

export function hitObjectWithBullet(bl) {
  for (const o of T.objs) {
    if (o.gone || !o.stopsShots) continue;
    const box = objBox(o);
    if (Math.abs(wd(bl.x - o.x)) < o.w / 2 + 4 && bl.y > box.top && bl.y < box.bottom) return o;
  }
  return null;
}

export function damageObject(o) {
  o.hp--; o.flash = .1;
  if (o.hp <= 0) { o.gone = true; return true; }
  return false;
}

export function updateObjects(dt) {
  for (const o of T.objs) {
    if (o.flash > 0) o.flash -= dt;
    if (o.squash > 0) o.squash = Math.max(0, o.squash - dt * 4);
  }
}

loadTerrain({});
```

- [ ] **Step 4: Kör testerna och bygget**

Run: `npm test 2>&1 | grep -E "FAIL|Tests"; npm run build 2>&1 | tail -1`
Expected: 117 tester PASS (112 + 5); bygget lyckas.

- [ ] **Step 5: Commit**

```bash
git add src/terrain.js test/terrain.test.js
git commit -m "Terrain: cave mode with ceiling, runtime holes, save/restore"
```

---

### Task 2: Grottdata i banfilerna

**Files:**
- Modify: `src/levels/meadows.json`, `dusk-valley.json`, `frost-coast.json`, `ember-woods.json` (nytt fält `caves`)
- Test: `test/levels.test.js`

**Interfaces:**
- Consumes: Task 1 (`loadTerrain` i grottläge, `heightAt`, `ceilingAt`)
- Produces: `LEVELS[i].caves` enligt Global Constraints. Grott-id: `m1 m2` (Meadows), `d1 d2 d3` (Dusk), `f1 f2` (Frost), `e1 e2` (Ember).

- [ ] **Step 1: Lägg till valideringstester i `test/levels.test.js`**

Ändra importen `import { loadTerrain, heightAt } from '../src/terrain.js';` till:

```js
import { loadTerrain, heightAt, ceilingAt } from '../src/terrain.js';
```

Lägg till inne i `describe.each(...)`-blocket, efter testet `'uses a known decor and known rules'`:

```js
  it('has one visible and one or two hidden caves with valid entrances', () => {
    const kinds = lv.caves.map(c => c.entrance.kind);
    expect(kinds.filter(k => k === 'hole')).toHaveLength(1);
    expect(kinds.length - 1).toBeGreaterThanOrEqual(1);
    expect(kinds.length - 1).toBeLessThanOrEqual(2);
    view.H = 540; loadTerrain(lv);
    for (const c of lv.caves) {
      expect(['hole', 'bush', 'rock', 'cliff']).toContain(c.entrance.kind);
      expect(['treasure', 'dark']).toContain(c.type);
      expect(c.entrance.x).toBeGreaterThanOrEqual(300);
      expect(c.entrance.x).toBeLessThan(L - 300);
      if (c.entrance.kind === 'cliff') {
        expect(c.entrance.y).toBeGreaterThanOrEqual(heightAt(c.entrance.x) + 60);
        expect(c.entrance.y).toBeLessThanOrEqual(heightAt(c.entrance.x) + 260);
      }
    }
  });
  it('keeps cave entrances clear of objects and zones', () => {
    const ew = { hole: 70, bush: 50, rock: 64, cliff: 40 };
    for (const c of lv.caves) {
      const w = ew[c.entrance.kind];
      for (const o of lv.objects) expect(Math.abs(c.entrance.x - o.x)).toBeGreaterThanOrEqual((w + (o.w || 44)) / 2 + 20);
      for (const z of lv.zones) expect(Math.abs(c.entrance.x - z.x)).toBeGreaterThanOrEqual((w + z.w) / 2 + 20);
    }
  });
  it('shapes every cave with room to bounce', () => {
    for (const c of lv.caves) {
      expect(c.floor).toHaveLength(c.width / 100 + 1);
      expect(c.ceiling).toHaveLength(c.width / 100 + 1);
      c.floor.forEach((f, i) => {
        expect(c.ceiling[i] - f).toBeGreaterThanOrEqual(180);
        expect(c.ceiling[i]).toBeLessThanOrEqual(430);
      });
    }
  });
  it('places cave loot inside the cave', () => {
    for (const c of lv.caves) {
      view.H = 540; loadTerrain({ width: c.width, ground: c.floor, ceiling: c.ceiling });
      for (const it of c.loot) {
        expect(['gem', 'star', 'life']).toContain(it.type);
        expect(it.x).toBeGreaterThanOrEqual(100);
        expect(it.x).toBeLessThanOrEqual(c.width - 100);
        expect(it.y).toBeGreaterThanOrEqual(heightAt(it.x) + 20);
        expect(it.y).toBeLessThanOrEqual(540 - ceilingAt(it.x) - 20);
      }
    }
  });
  it('hides exactly one extra life per world', () => {
    expect(lv.caves.flatMap(c => c.loot).filter(it => it.type === 'life')).toHaveLength(1);
  });
```

och sist i filen:

```js
describe('caves across worlds', () => {
  it('have unique ids', () => {
    const ids = LEVELS.flatMap(l => l.caves.map(c => c.id));
    expect(new Set(ids).size).toBe(ids.length);
  });
});
```

- [ ] **Step 2: Kör och se dem fallera**

Run: `npm test 2>&1 | grep -E "FAIL|Tests"`
Expected: FAIL – `lv.caves` är `undefined`.

- [ ] **Step 3: Lägg till `caves` i banfilerna**

Tre golv/tak-mallar används (A: bredd 1400, B: 1200, C: 1600):

- A floor `[80, 80, 90, 110, 130, 120, 100, 80, 70, 80, 100, 120, 110, 90, 80]`, ceiling `[330, 340, 360, 380, 390, 370, 340, 320, 310, 330, 360, 380, 370, 350, 330]`
- B floor `[70, 75, 90, 70, 60, 80, 110, 140, 120, 90, 70, 70, 70]`, ceiling `[300, 320, 340, 330, 300, 290, 320, 360, 380, 360, 330, 310, 300]`
- C floor `[90, 90, 80, 70, 70, 90, 120, 150, 150, 120, 90, 70, 60, 70, 90, 100, 100]`, ceiling `[340, 350, 360, 360, 340, 330, 350, 380, 390, 380, 350, 320, 310, 330, 360, 370, 360]`

I varje banfil, lägg till fältet `"caves"` efter `"decor"` (kom ihåg kommatecknet efter `"decor": "…"`).

`src/levels/meadows.json`:

```json
  "caves": [
    { "id": "m1", "entrance": { "kind": "hole", "x": 1850, "w": 70 }, "type": "treasure", "width": 1400,
      "floor": [80, 80, 90, 110, 130, 120, 100, 80, 70, 80, 100, 120, 110, 90, 80],
      "ceiling": [330, 340, 360, 380, 390, 370, 340, 320, 310, 330, 360, 380, 370, 350, 330],
      "objects": [{ "type": "mushroom", "x": 700 }],
      "loot": [
        { "type": "star", "x": 300, "y": 200 }, { "type": "gem", "x": 520, "y": 230 }, { "type": "star", "x": 700, "y": 280 },
        { "type": "star", "x": 950, "y": 200 }, { "type": "life", "x": 1150, "y": 230 }, { "type": "gem", "x": 1250, "y": 200 }
      ] },
    { "id": "m2", "entrance": { "kind": "bush", "x": 3000 }, "type": "dark", "width": 1200,
      "floor": [70, 75, 90, 70, 60, 80, 110, 140, 120, 90, 70, 70, 70],
      "ceiling": [300, 320, 340, 330, 300, 290, 320, 360, 380, 360, 330, 310, 300],
      "loot": [
        { "type": "star", "x": 250, "y": 170 }, { "type": "star", "x": 450, "y": 180 }, { "type": "gem", "x": 650, "y": 230 },
        { "type": "star", "x": 850, "y": 250 }, { "type": "star", "x": 1000, "y": 200 }, { "type": "gem", "x": 1100, "y": 180 }
      ],
      "foes": [{ "x": 400, "y": 220 }, { "x": 700, "y": 260 }, { "x": 950, "y": 240 }] }
  ]
```

`src/levels/dusk-valley.json`:

```json
  "caves": [
    { "id": "d1", "entrance": { "kind": "hole", "x": 600, "w": 70 }, "type": "treasure", "width": 1600,
      "floor": [90, 90, 80, 70, 70, 90, 120, 150, 150, 120, 90, 70, 60, 70, 90, 100, 100],
      "ceiling": [340, 350, 360, 360, 340, 330, 350, 380, 390, 380, 350, 320, 310, 330, 360, 370, 360],
      "objects": [{ "type": "cloud", "x": 800, "y": 260, "w": 100 }],
      "loot": [
        { "type": "star", "x": 300, "y": 180 }, { "type": "gem", "x": 600, "y": 200 }, { "type": "star", "x": 800, "y": 330 },
        { "type": "star", "x": 1000, "y": 260 }, { "type": "gem", "x": 1300, "y": 180 }, { "type": "star", "x": 1450, "y": 200 }
      ] },
    { "id": "d2", "entrance": { "kind": "rock", "x": 2000 }, "type": "dark", "width": 1400,
      "floor": [80, 80, 90, 110, 130, 120, 100, 80, 70, 80, 100, 120, 110, 90, 80],
      "ceiling": [330, 340, 360, 380, 390, 370, 340, 320, 310, 330, 360, 380, 370, 350, 330],
      "loot": [
        { "type": "star", "x": 300, "y": 190 }, { "type": "gem", "x": 550, "y": 260 }, { "type": "star", "x": 800, "y": 180 },
        { "type": "star", "x": 1000, "y": 230 }, { "type": "gem", "x": 1250, "y": 200 }
      ],
      "foes": [{ "x": 450, "y": 240 }, { "x": 750, "y": 240 }, { "x": 1100, "y": 240 }] },
    { "id": "d3", "entrance": { "kind": "cliff", "x": 4450, "y": 345 }, "type": "treasure", "width": 1200,
      "floor": [70, 75, 90, 70, 60, 80, 110, 140, 120, 90, 70, 70, 70],
      "ceiling": [300, 320, 340, 330, 300, 290, 320, 360, 380, 360, 330, 310, 300],
      "loot": [
        { "type": "star", "x": 250, "y": 170 }, { "type": "star", "x": 500, "y": 200 }, { "type": "life", "x": 700, "y": 260 },
        { "type": "gem", "x": 900, "y": 230 }, { "type": "star", "x": 1050, "y": 180 }
      ] }
  ]
```

`src/levels/frost-coast.json`:

```json
  "caves": [
    { "id": "f1", "entrance": { "kind": "hole", "x": 1850, "w": 70 }, "type": "dark", "width": 1600,
      "floor": [90, 90, 80, 70, 70, 90, 120, 150, 150, 120, 90, 70, 60, 70, 90, 100, 100],
      "ceiling": [340, 350, 360, 360, 340, 330, 350, 380, 390, 380, 350, 320, 310, 330, 360, 370, 360],
      "loot": [
        { "type": "star", "x": 350, "y": 190 }, { "type": "gem", "x": 700, "y": 260 }, { "type": "star", "x": 950, "y": 270 },
        { "type": "gem", "x": 1200, "y": 180 }, { "type": "star", "x": 1450, "y": 200 }
      ],
      "foes": [{ "x": 500, "y": 250 }, { "x": 900, "y": 250 }, { "x": 1300, "y": 250 }] },
    { "id": "f2", "entrance": { "kind": "bush", "x": 3200 }, "type": "treasure", "width": 1400,
      "floor": [80, 80, 90, 110, 130, 120, 100, 80, 70, 80, 100, 120, 110, 90, 80],
      "ceiling": [330, 340, 360, 380, 390, 370, 340, 320, 310, 330, 360, 380, 370, 350, 330],
      "objects": [{ "type": "mushroom", "x": 1000 }],
      "loot": [
        { "type": "star", "x": 300, "y": 200 }, { "type": "gem", "x": 500, "y": 240 }, { "type": "star", "x": 800, "y": 200 },
        { "type": "star", "x": 1000, "y": 340 }, { "type": "life", "x": 1200, "y": 220 }
      ] }
  ]
```

`src/levels/ember-woods.json`:

```json
  "caves": [
    { "id": "e1", "entrance": { "kind": "hole", "x": 3000, "w": 70 }, "type": "treasure", "width": 1600,
      "floor": [90, 90, 80, 70, 70, 90, 120, 150, 150, 120, 90, 70, 60, 70, 90, 100, 100],
      "ceiling": [340, 350, 360, 360, 340, 330, 350, 380, 390, 380, 350, 320, 310, 330, 360, 370, 360],
      "objects": [{ "type": "rock", "x": 700, "w": 60, "h": 40 }],
      "loot": [
        { "type": "star", "x": 300, "y": 190 }, { "type": "gem", "x": 700, "y": 260 }, { "type": "star", "x": 1000, "y": 240 },
        { "type": "life", "x": 1250, "y": 180 }, { "type": "gem", "x": 1450, "y": 200 }
      ] },
    { "id": "e2", "entrance": { "kind": "rock", "x": 500 }, "type": "dark", "width": 1200,
      "floor": [70, 75, 90, 70, 60, 80, 110, 140, 120, 90, 70, 70, 70],
      "ceiling": [300, 320, 340, 330, 300, 290, 320, 360, 380, 360, 330, 310, 300],
      "loot": [
        { "type": "star", "x": 300, "y": 170 }, { "type": "gem", "x": 550, "y": 170 }, { "type": "star", "x": 800, "y": 280 },
        { "type": "star", "x": 1000, "y": 220 }
      ],
      "foes": [{ "x": 450, "y": 230 }, { "x": 700, "y": 230 }, { "x": 950, "y": 230 }] }
  ]
```

- [ ] **Step 4: Kör testerna**

Run: `npm test 2>&1 | grep -E "FAIL|Tests"; npm run build 2>&1 | tail -1`
Expected: 138 tester PASS (117 + 5×4 + 1); bygget lyckas.

- [ ] **Step 5: Commit**

```bash
git add src/levels test/levels.test.js
git commit -m "Add cave data to every world"
```

---

### Task 3: Grottlogik – ingångar, scenbyte, skatter, utgång

**Files:**
- Create: `src/caves.js`
- Modify: `src/state.js`, `src/game.js`, `src/player.js`, `src/pickups.js`
- Test: `test/caves.test.js` (ny), `test/player.test.js`, `test/playability.test.js`

**Interfaces:**
- Consumes: Task 1 (terräng), Task 2 (data), `loadZones`, `loadRules`, `curLevel`, `collectGem`
- Produces:
  - `caveById(id): cave | null` (i aktuell värld)
  - `surfaceLevel(level): level` – kopia med ingångsobjekt/hål för grottor som inte finns i `G.cavesUsed`
  - `breakEntrance(o)` – öppnar hål för förstörd `bush`/`rock`-ingång
  - `enterCave(cave)`, `exitCave()`
  - `tryCliff(o)` – går in om bollen är i höjd med öppningen
  - `updateCaves()` – fall ner i hål → in; i grotta: plocka skatter, utgång vid `width − 60`
  - `collectGem(g)` i `pickups.js`

- [ ] **Step 1: Skriv `test/caves.test.js`**

```js
import { describe, it, expect } from 'vitest';
import { G, view } from '../src/state.js';
import { newGame, startLevel, update } from '../src/game.js';
import { groundAt, objects, damageObject, caveWidth, holeAt, openHole } from '../src/terrain.js';
import { enterCave, breakEntrance, updateCaves } from '../src/caves.js';
import { updatePlayer } from '../src/player.js';
import { LEVELS } from '../src/levels/index.js';

function world(i) { view.H = 540; view.W = 960; newGame('play'); G.level = i; startLevel(); G.P.inv = 0; }
const caveOf = (i, kind) => LEVELS[i].caves.find(c => c.entrance.kind === kind);
const lifeCave = i => LEVELS[i].caves.find(c => c.loot.some(l => l.type === 'life'));

describe('cave entrances', () => {
  it('opens every cave entrance in play', () => {
    for (let i = 0; i < LEVELS.length; i++) {
      world(i);
      for (const c of LEVELS[i].caves) {
        if (c.entrance.kind === 'hole') expect(groundAt(c.entrance.x)).toBeNull();
        else expect(objects().some(o => o.cave === c.id)).toBe(true);
      }
    }
  });
  it('shows no entrances behind the menu', () => {
    view.H = 540; view.W = 960; newGame('menu');
    expect(groundAt(caveOf(0, 'hole').entrance.x)).not.toBeNull();
    expect(objects().some(o => o.cave)).toBe(false);
  });
  it('opens a hidden passage when a bush is shot', () => {
    world(0);
    const c = caveOf(0, 'bush'), o = objects().find(o => o.cave === c.id);
    expect(damageObject(o)).toBe(true);
    breakEntrance(o);
    expect(groundAt(o.x)).toBeNull();
    expect(holeAt(o.x)).toMatchObject({ cave: c.id });
  });
  it('needs three hits to crack a rock entrance', () => {
    world(1);
    const o = objects().find(o => o.cave === caveOf(1, 'rock').id);
    expect(damageObject(o)).toBe(false);
    expect(damageObject(o)).toBe(false);
    expect(damageObject(o)).toBe(true);
  });
  it('enters a cliff only through its opening', () => {
    const c = caveOf(1, 'cliff');
    for (const [dy, scene] of [[0, 'cave'], [100, 'surface']]) {
      world(1);
      const o = objects().find(o => o.cave === c.id);
      G.own.anti = true;
      Object.assign(G.P, { x: o.x - 20 - 18 + 2, y: view.H - c.entrance.y + dy, vx: 300, vy: 0 });
      updatePlayer(1 / 60);
      expect(G.scene).toBe(scene);
    }
  });
});

describe('entering and leaving', () => {
  it('drops into the cave through its hole', () => {
    world(0);
    const c = caveOf(0, 'hole');
    Object.assign(G.P, { x: c.entrance.x, y: view.H + 30 });
    updateCaves();
    expect(G.scene).toBe('cave');
    expect(G.cave.id).toBe(c.id);
    expect(caveWidth()).toBe(c.width);
  });
  it('leaves the surface exactly as it was', () => {
    world(2);
    const crystal = objects().find(o => o.type === 'crystal');
    damageObject(crystal); damageObject(crystal); damageObject(crystal);
    const enemies = [{ type: 'float', x: 5, y: 5 }], drops = [{ x: 1, y: 1, c: 0 }], embers = [{ x: 2, y: 2 }];
    Object.assign(G, { enemies, drops, embers, camX: 1234 });
    enterCave(caveOf(2, 'hole'));
    G.P.x = G.cave.width - 30;
    updateCaves();
    expect(G.scene).toBe('surface');
    expect(G.enemies).toBe(enemies);
    expect(G.drops).toBe(drops);
    expect(G.embers).toBe(embers);
    expect(G.camX).toBe(1234);
    expect(objects().find(o => o.type === 'crystal').gone).toBe(true);
  });
  it('returns the ball to a closed entrance', () => {
    world(0);
    const c = caveOf(0, 'hole');
    enterCave(c);
    G.P.x = c.width - 30;
    updateCaves();
    expect(G.P.x).toBe(c.entrance.x);
    expect(G.P.inv).toBe(2);
    expect(groundAt(c.entrance.x)).not.toBeNull();
    Object.assign(G.P, { y: view.H + 30, inv: 0 });
    updateCaves();
    expect(G.scene).toBe('surface');
    expect(G.P.y).toBeLessThan(view.H);
  });
  it('counts every cave found', () => {
    world(0);
    expect(G.cavesUsed.size).toBe(0);
    enterCave(caveOf(0, 'bush'));
    expect(G.cavesUsed.size).toBe(1);
  });
});

describe('inside a cave', () => {
  it('collects stars, gems and the extra life', () => {
    world(0);
    enterCave(lifeCave(0));
    const lives = G.lives, score = G.score;
    for (const it of [...G.loot]) { G.P.x = it.x; G.P.y = view.H - it.y; updateCaves(); }
    expect(G.loot).toHaveLength(0);
    expect(G.lives).toBe(lives + 1);
    expect(G.score).toBeGreaterThan(score);
    expect(G.lifeTaken).toBe(true);
  });
  it('hides the extra life once it has been taken', () => {
    world(0);
    G.lifeTaken = true;
    enterCave(lifeCave(0));
    expect(G.loot.some(l => l.type === 'life')).toBe(false);
  });
  it('spawns no enemy waves inside a cave', () => {
    world(0);
    enterCave(caveOf(0, 'hole'));
    G.spawnT = 0;
    update(1 / 60);
    expect(G.enemies).toHaveLength(0);
  });
  it('keeps dark cave foes however far they are from the camera', () => {
    world(0);
    const c = LEVELS[0].caves.find(c => c.type === 'dark');
    enterCave(c);
    G.P.inv = 99;
    for (let i = 0; i < 360; i++) update(1 / 60);
    expect(G.enemies).toHaveLength(c.foes.length);
  });
  it('removes drops that fall into a hole', () => {
    world(0);
    openHole(1000, 70, null);
    G.drops = [{ x: 1000, y: view.H + 50, vx: 0, vy: 80, t: 0, c: 0 }];
    update(1 / 60);
    expect(G.drops).toHaveLength(0);
  });
});
```

I `test/player.test.js`, lägg till sist i `describe`-blocket:

```js
  it('stops at a cave ceiling', () => {
    setup([], { x: 600, y: 262, vy: -600 }, { width: 1200, ground: Array(13).fill(100), ceiling: Array(13).fill(300) });
    updatePlayer(1 / 60);
    expect(G.P.y).toBe(540 - 300 + 18);
    expect(G.P.vy).toBe(0);
  });
  it('bounces off the far cave wall', () => {
    setup([], { x: 1195, y: 300, vx: 300, spin: 1 }, { width: 1200, ground: Array(13).fill(100), ceiling: Array(13).fill(300) });
    updatePlayer(1 / 60);
    expect(G.P.x).toBe(1200 - 18);
    expect(G.P.vx).toBeLessThan(0);
  });
```

I `test/playability.test.js`, lägg till importen `import { ceilingAt } from '../src/terrain.js';` (utöka befintlig import från `../src/terrain.js` till `{ loadTerrain, groundAt, ceilingAt }`), och sist i filen:

```js
function crossCave(c, fps) {
  view.H = 540; loadTerrain({ width: c.width, ground: c.floor, ceiling: c.ceiling, objects: c.objects || [] }); loadZones({}); loadRules({});
  Object.assign(G, { state: 'play', scene: 'cave', dead: 0, shield: 0, lives: 3, t: 0, parts: [], own: { thrust: false, anti: false, rapid: false, double: false, sat: false } });
  G.P = { x: 80, y: ceilingAt(80) + 40, vx: 60, vy: 0, r: 18, spin: 1, ang: 0, face: 1, inv: 0 };
  keys.ArrowRight = true;
  for (let i = 0; i < 30 * fps; i++) {
    updatePlayer(1 / fps);
    if (G.dead > 0) return 'dead';
    if (G.P.x > c.width - 60) return 'passed';
  }
  return 'stuck';
}

describe('caves in spin mode', () => {
  it.each(LEVELS.flatMap(lv => lv.caves.map(c => [c.id, c])))('cave %s can be crossed in spin mode', (_, c) => {
    expect(crossCave(c, 60)).toBe('passed');
    expect(crossCave(c, 30)).toBe('passed');
  });
});
```

- [ ] **Step 2: Kör och se dem fallera**

Run: `npm test 2>&1 | grep -E "FAIL|Tests"`
Expected: FAIL – `../src/caves.js` saknas; tak-/väggtesterna fallerar.

- [ ] **Step 3: Fält i `src/state.js`**

Ändra raden `camX: 0, shake: 0, paused: false, best: 0,` till:

```js
  camX: 0, shake: 0, paused: false, best: 0,
  scene: 'surface', cave: null, surfaceSnapshot: null, cavesUsed: new Set(), lifeTaken: false, loot: [],
```

- [ ] **Step 4: `collectGem` och fallande droppar i `src/pickups.js`**

Ändra `import { G } from './state.js';` till `import { G, view } from './state.js';`.

Lägg till före `function updatePickups`:

```js
export function collectGem(g) {
  G.sel = (G.sel + 1) % SLOTS.length; G.score += 40; sfx('gem');
  addText(g.x, g.y - 10, SLOTS[G.sel].label, '#b8ffea');
}
```

I `updatePickups`, direkt efter raden `const s = surfaceBelow(d.x, prevY + 9); if (s && d.y > s.y - 9) { … }`:

```js
    if (d.y > view.H + 40) { d.gone = true; continue; }
```

Ersätt i `updateAllPickups`

```js
  G.gems = updatePickups(G.gems, dt, g => {
    G.sel = (G.sel + 1) % SLOTS.length; G.score += 40; sfx('gem');
    addText(g.x, g.y - 10, SLOTS[G.sel].label, '#b8ffea');
  });
```

med

```js
  G.gems = updatePickups(G.gems, dt, collectGem);
```

- [ ] **Step 5: Skapa `src/caves.js`**

```js
import { TOP } from './config.js';
import { G, view } from './state.js';
import { rnd, wd } from './util.js';
import { loadTerrain, saveTerrain, restoreTerrain, openHole, closeHole, holeAt, groundAt, ceilingAt, caveWidth, objBox } from './terrain.js';
import { loadZones } from './zones.js';
import { loadRules } from './rules.js';
import { sfx } from './audio.js';
import { banner, burst, addText } from './fx.js';
import { curLevel } from './game.js';
import { collectGem } from './pickups.js';

const EXIT = 60;
const TITLES = { treasure: 'Treasure cave', dark: 'Dark cave' };
const ENTRANCE = {
  hole:  e => ({ type: 'hole', x: e.x, w: e.w || 70 }),
  bush:  e => ({ type: 'bush', x: e.x, hp: 1, stopsShots: true }),
  rock:  e => ({ type: 'rock', x: e.x, w: 64, h: 46, hp: 3, stopsShots: true, crack: true }),
  cliff: e => ({ type: 'cliff', x: e.x, opening: e.y }),
};

export const caveById = id => (curLevel().caves || []).find(c => c.id === id) || null;

export function surfaceLevel(lv) {
  const entrances = (lv.caves || []).filter(c => !G.cavesUsed.has(c.id)).map(c => ({ ...ENTRANCE[c.entrance.kind](c.entrance), cave: c.id }));
  return { ...lv, objects: [...lv.objects, ...entrances] };
}

export function breakEntrance(o) {
  const top = objBox(o).top;
  openHole(o.x, 70, o.cave);
  burst(o.x, top + o.h / 2, '#c9b28a', 24); sfx('pop');
  addText(o.x, top, 'A hidden passage!', '#ffe066');
}

export function enterCave(cave) {
  G.surfaceSnapshot = { terrain: saveTerrain(), enemies: G.enemies, bullets: G.bullets, ebullets: G.ebullets, drops: G.drops, gems: G.gems, embers: G.embers, camX: G.camX, spawnT: G.spawnT };
  G.scene = 'cave'; G.cave = cave; G.cavesUsed.add(cave.id);
  loadTerrain({ width: cave.width, ground: cave.floor, ceiling: cave.ceiling, objects: cave.objects || [] });
  loadZones({}); loadRules({});
  G.enemies = (cave.foes || []).map(f => ({ type: 'float', x: f.x, y: view.H - f.y, baseY: view.H - f.y, vx: 0, vy: 0, t: rnd(0, 3), age: 0, r: 14, hp: 1, color: -1, phase: 0, flash: 0, dead: false, keep: true }));
  G.bullets = []; G.ebullets = []; G.drops = []; G.gems = [];
  G.loot = cave.loot.filter(l => l.type !== 'life' || !G.lifeTaken).map(l => ({ ...l }));
  Object.assign(G.P, { x: 80, y: ceilingAt(80) + 40, vx: 60, vy: 0, inv: 1 });
  G.camX = 0;
  banner(TITLES[cave.type]); sfx('power');
}

export function exitCave() {
  const s = G.surfaceSnapshot, cave = G.cave;
  loadZones(curLevel()); loadRules(curLevel());
  restoreTerrain(s.terrain); closeHole(cave.id);
  Object.assign(G, { scene: 'surface', cave: null, surfaceSnapshot: null, loot: [], enemies: s.enemies, bullets: s.bullets, ebullets: s.ebullets, drops: s.drops, gems: s.gems, embers: s.embers, camX: s.camX, spawnT: s.spawnT });
  const x = cave.entrance.x + (cave.entrance.kind === 'cliff' ? 70 : 0);
  Object.assign(G.P, { x, y: (groundAt(x) ?? view.H) - 140, vx: 0, vy: 0, inv: 2 });
  banner('Back to the surface'); sfx('gem');
}

export function tryCliff(o) {
  if (o.cave && !G.cavesUsed.has(o.cave) && Math.abs(G.P.y - (view.H - o.opening)) < 26) enterCave(caveById(o.cave));
}

function collect(it) {
  const y = view.H - it.y;
  if (it.type === 'gem') collectGem({ x: it.x, y });
  else if (it.type === 'star') { G.score += 200; addText(it.x, y, '+200', '#ffe066'); sfx('drop'); }
  else if (it.type === 'life') { G.lives++; G.lifeTaken = true; addText(it.x, y, 'Extra life!', '#ffcf4a'); sfx('power'); }
}

export function updateCaves() {
  const P = G.P;
  if (G.dead > 0 || G.state !== 'play') return;
  if (G.scene === 'surface') {
    if (P.y - P.r <= view.H) return;
    const cave = caveById(holeAt(P.x)?.cave);
    if (cave) enterCave(cave);
    else Object.assign(P, { x: P.x + 100, y: TOP + 90, vy: 0 });
    return;
  }
  for (const it of G.loot) {
    const dx = wd(it.x - P.x), dy = view.H - it.y - P.y;
    if (dx * dx + dy * dy < (P.r + 12) ** 2) { it.got = true; collect(it); }
  }
  G.loot = G.loot.filter(it => !it.got);
  if (P.x > caveWidth() - EXIT) exitCave();
}
```

- [ ] **Step 6: Koppla in i `src/game.js`**

Lägg till importer:

```js
import { surfaceLevel, breakEntrance, updateCaves } from './caves.js';
```

och utöka terräng-importen till `import { loadTerrain, hitObjectWithBullet, damageObject, objBox, updateObjects, caveWidth } from './terrain.js';` samt util-importen till `import { lerp, rnd, wd, clamp } from './util.js';`.

I `startLevel()`, ersätt raden `loadTerrain(curLevel());` med:

```js
  Object.assign(G, { scene: 'surface', cave: null, surfaceSnapshot: null, cavesUsed: new Set(), lifeTaken: false, loot: [] });
  loadTerrain(G.state === 'menu' ? curLevel() : surfaceLevel(curLevel()));
```

I `update`, ändra spawnvillkoret `if (G.spawnT <= 0 && G.enemies.length < 7 + G.level * 2)` till:

```js
      if (G.spawnT <= 0 && G.scene === 'surface' && G.enemies.length < 7 + G.level * 2)
```

I skott-loopen, ersätt

```js
          if (damageObject(o)) {
            const top = objBox(o).top;
            G.drops.push(pickup(o.x - 6, top, { c: o.c }), pickup(o.x + 6, top, { c: o.c }));
            burst(o.x, top + o.h / 2, COLORS[o.c], 20); sfx('pop');
            G.score += 30; addText(o.x, top, '+30');
          } else { burst(b.x, b.y, '#ffffff', 4, 120); sfx('clink'); }
```

med

```js
          if (damageObject(o)) {
            if (o.cave) breakEntrance(o);
            else {
              const top = objBox(o).top;
              G.drops.push(pickup(o.x - 6, top, { c: o.c }), pickup(o.x + 6, top, { c: o.c }));
              burst(o.x, top + o.h / 2, COLORS[o.c], 20); sfx('pop');
              G.score += 30; addText(o.x, top, '+30');
            }
          } else { burst(b.x, b.y, '#ffffff', 4, 120); sfx('clink'); }
```

Ersätt fiendefiltret

```js
    G.enemies = G.enemies.filter(e => !e.dead && !(e.age > 4 && Math.abs(wd(e.x - cx)) > view.W / 2 + 520));
```

med

```js
    G.enemies = G.enemies.filter(e => !e.dead && e.y < view.H + 60 && (e.keep || !(e.age > 4 && Math.abs(wd(e.x - cx)) > view.W / 2 + 520)));
```

Direkt efter `updateAllPickups(dt);`:

```js
    updateCaves();
```

Efter kameraraden `G.camX += (tx - G.camX) * Math.min(1, 3 * dt);`:

```js
  const cw = caveWidth();
  if (cw != null) G.camX = cw > view.W ? clamp(G.camX, 0, cw - view.W) : (cw - view.W) / 2;
```

- [ ] **Step 7: Tak, väggar och klippa i `src/player.js`**

Ändra terräng-importen till:

```js
import { surfaceBelow, collideCircle, touchesHazard, hazardBelow, ceilingAt, caveWidth } from './terrain.js';
```

och lägg till `import { tryCliff } from './caves.js';`.

Ersätt `collideCircle(P, prevBottom);` med:

```js
  const hit = collideCircle(P, prevBottom);
  if (hit?.type === 'cliff') { tryCliff(hit); if (G.scene === 'cave') return; }
```

Ersätt `if (P.y - P.r < TOP) { P.y = TOP + P.r; if (P.vy < 0) P.vy = 0; }` med:

```js
  const ceil = ceilingAt(P.x);
  if (P.y - P.r < ceil) { P.y = ceil + P.r; if (P.vy < 0) P.vy = 0; }
  const cw = caveWidth();
  if (cw != null) {
    if (P.x < P.r) { P.x = P.r; P.vx = Math.abs(P.vx) * .6; }
    else if (P.x > cw - P.r) { P.x = cw - P.r; P.vx = -Math.abs(P.vx) * .6; }
  }
```

- [ ] **Step 8: Kör testerna och bygget**

Run: `npm test 2>&1 | grep -E "FAIL|Tests"; npm run build 2>&1 | tail -1`
Expected: 163 tester PASS (138 + 14 grottor + 2 spelare + 9 grottspelbarhet); bygget lyckas.

- [ ] **Step 9: Commit**

```bash
git add src test
git commit -m "Caves: entrances, scene switch with surface snapshot, loot and exit"
```

---

### Task 4: Rendering, röktest och dokumentation

**Files:**
- Modify: `src/render.js`, `src/hud.js`
- Modify: `CLAUDE.md`, `docs/superpowers/specs/2026-09-25-world-variation-design.md`

**Interfaces:**
- Consumes: `holes`, `ceilingAt`, `caveWidth` (terrain), `G.scene`, `G.cave`, `G.loot`, `G.cavesUsed`
- Produces: –

- [ ] **Step 1: Hål och ingångsobjekt i `src/render.js`**

Utöka terräng-importen till `import { groundAt, surfaceBelow, objects, objBox, holes, ceilingAt, caveWidth } from './terrain.js';`.

I `drawWorld`, direkt efter `ctx.lineTo(W + 38, H + 30); ctx.fill();` (markfyllningen, före gräskanten):

```js
  ctx.fillStyle = '#0b0810';
  for (const h of holes()) {
    const x0 = sx(h.x - h.w / 2);
    if (x0 > W + 40 || x0 + h.w < -40) continue;
    ctx.fillRect(x0, Math.min(gAt(x0 - 2), gAt(x0 + h.w + 2)), h.w, H);
  }
```

I `drawObjects`, ändra första raden till `const { ctx, W } = view, s = G.sat, pal = curLevel().palette;`, lägg till sprickor i `rock`-grenen direkt efter `ctx.fillStyle = tint('#b9bec8', s); rrect(ctx, l + 5, top + 4, o.w - 10, 7, 3.5); ctx.fill();`:

```js
      if (o.crack) {
        ctx.strokeStyle = 'rgba(20,16,28,.75)'; ctx.lineWidth = 2; ctx.beginPath();
        ctx.moveTo(x - 4, top + 6); ctx.lineTo(x + 3, top + 18); ctx.lineTo(x - 2, top + 28); ctx.lineTo(x + 6, top + 40);
        if (o.hp < 3) { ctx.moveTo(x + 3, top + 18); ctx.lineTo(x + 14, top + 22); }
        if (o.hp < 2) { ctx.moveTo(x - 2, top + 28); ctx.lineTo(x - 14, top + 34); }
        ctx.stroke();
      }
```

och lägg till två grenar före den avslutande `}` i if-kedjan (efter `crystal`-grenen):

```js
    } else if (o.type === 'bush') {
      ctx.fillStyle = tint(pal.grass, s * .8); ctx.beginPath();
      ctx.arc(x - 13, top + 22, 13, 0, TAU); ctx.arc(x + 12, top + 21, 14, 0, TAU); ctx.arc(x, top + 13, 15, 0, TAU); ctx.fill();
      ctx.fillStyle = tint(pal.deco, s); ctx.beginPath(); ctx.arc(x - 6, top + 10, 2.5, 0, TAU); ctx.arc(x + 9, top + 16, 2.5, 0, TAU); ctx.fill();
    } else if (o.type === 'cliff') {
      ctx.fillStyle = tint('#6b5d52', s); ctx.fillRect(l, top, o.w, o.h + 8);
      ctx.fillStyle = tint('#54483f', s); for (let y = top + 14; y < top + o.h; y += 26) ctx.fillRect(l + (Math.round(y / 26) % 2 ? 6 : 0), y, o.w - 6, 3);
      if (o.cave && !G.cavesUsed.has(o.cave)) { ctx.fillStyle = '#0b0810'; ctx.beginPath(); ctx.ellipse(x, view.H - o.opening, o.w / 2 - 4, 22, 0, 0, TAU); ctx.fill(); }
```

(Obs: den befintliga kristallgrenens avslutande rad `ctx.stroke();` följs av `}` – de nya grenarna läggs in så att kedjan blir `… } else if (o.type === 'crystal') { … } else if (o.type === 'bush') { … } else if (o.type === 'cliff') { … }`.)

- [ ] **Step 2: Grottscen, skatter och mörker i `src/render.js`**

Lägg till efter `drawWorld`:

```js
function drawCave() {
  const { ctx, W, H } = view, camX = G.camX, pal = curLevel().palette, s = Math.max(G.sat, .5), cw = caveWidth();
  const cAt = x => ceilingAt(camX + x), gAt = x => groundAt(camX + x) ?? H + 40;
  ctx.fillStyle = '#120f1a'; ctx.fillRect(-30, -30, W + 60, H + 60);
  ctx.fillStyle = tint(pal.ground, s);
  ctx.beginPath(); ctx.moveTo(-30, -30);
  for (let x = -30; x <= W + 38; x += 6) ctx.lineTo(x, cAt(x));
  ctx.lineTo(W + 38, -30); ctx.fill();
  ctx.beginPath(); ctx.moveTo(-30, H + 30);
  for (let x = -30; x <= W + 38; x += 6) ctx.lineTo(x, gAt(x));
  ctx.lineTo(W + 38, H + 30); ctx.fill();
  ctx.strokeStyle = tint(pal.grass, s * .6); ctx.lineWidth = 3; ctx.beginPath();
  for (let x = -30; x <= W + 38; x += 6) x === -30 ? ctx.moveTo(x, gAt(x)) : ctx.lineTo(x, gAt(x));
  ctx.stroke();
  ctx.fillRect(-30, -30, -camX + 30, H + 60);
  ctx.fillRect(cw - camX, -30, W + 60, H + 60);
  const bx = cw - 30 - camX, top = cAt(bx), g = ctx.createLinearGradient(bx - 30, 0, bx + 30, 0);
  g.addColorStop(0, 'rgba(255,245,200,0)'); g.addColorStop(.5, `rgba(255,245,200,${.45 + .15 * Math.sin(G.t * 4)})`); g.addColorStop(1, 'rgba(255,245,200,0)');
  ctx.fillStyle = g; ctx.fillRect(bx - 30, top, 60, gAt(bx) - top);
}

function drawLoot() {
  const { ctx, H } = view;
  for (const it of G.loot) {
    const x = sx(it.x), y = H - it.y + Math.sin(G.t * 3 + it.x) * 4;
    if (it.type === 'gem') {
      ctx.fillStyle = 'rgba(184,255,234,.25)'; ctx.beginPath(); ctx.arc(x, y, 13, 0, TAU); ctx.fill();
      drawGemShape(x, y, 9, G.t);
    } else if (it.type === 'star') {
      ctx.fillStyle = '#ffe066'; ctx.beginPath();
      for (let i = 0; i < 10; i++) { const a = -Math.PI / 2 + i * Math.PI / 5, r = i % 2 ? 4.5 : 11; ctx.lineTo(x + Math.cos(a) * r, y + Math.sin(a) * r); }
      ctx.closePath(); ctx.fill();
    } else if (it.type === 'life') {
      const g = ctx.createRadialGradient(x - 3, y - 4, 1, x, y, 11);
      g.addColorStop(0, '#fff8dc'); g.addColorStop(.5, '#ffcf4a'); g.addColorStop(1, '#d9642a');
      ctx.fillStyle = 'rgba(255,207,74,.3)'; ctx.beginPath(); ctx.arc(x, y, 17, 0, TAU); ctx.fill();
      ctx.fillStyle = g; ctx.beginPath(); ctx.arc(x, y, 11, 0, TAU); ctx.fill();
    }
  }
}

function drawDarkness() {
  const { ctx, W, H } = view, x = sx(G.P.x), y = G.P.y;
  const g = ctx.createRadialGradient(x, y, 50, x, y, 130);
  g.addColorStop(0, 'rgba(0,0,0,0)'); g.addColorStop(1, 'rgba(0,0,0,.97)');
  ctx.fillStyle = g; ctx.fillRect(-30, -30, W + 60, H + 60);
}
```

I `render()`, ersätt

```js
  drawWorld();
  drawZones('ground');
  drawObjects();
  drawPickups();
```

med

```js
  if (G.scene === 'cave') drawCave(); else { drawWorld(); drawZones('ground'); }
  drawObjects();
  drawPickups();
  drawLoot();
```

och direkt före `ctx.restore();` sist i `render()`:

```js
  if (G.scene === 'cave' && G.cave.type === 'dark') drawDarkness();
```

- [ ] **Step 3: "Secrets found" i `src/hud.js`**

Direkt före `if (G.paused) {`:

```js
  const caves = curLevel().caves || [];
  if (G.state === 'clear' && caves.length) {
    ctx.textAlign = 'center'; ctx.font = `400 14px ${HUDF}`; ctx.fillStyle = 'rgba(255,255,255,.85)';
    ctx.fillText(`Secrets found: ${G.cavesUsed.size}/${caves.length}`, W / 2, H * .33 + 66);
  }
```

- [ ] **Step 4: Bygg och testa**

Run: `npm run build 2>&1 | tail -1; npm test 2>&1 | grep -E "FAIL|Tests"`
Expected: bygget lyckas; 163 tester PASS.

- [ ] **Step 5: Headless-röktest och skärmbilder**

Skapa temporärt `smoke.html` (committas inte) med samma markup-id:n som `index.html` (`c`, `touch`, `zone`, `base`, `knob`, `bPow`, `bFire`, `menu`, `start`, `bestMenu`, `over`, `overText`, `again`), som laddar `./src/main.js` och kör:

```html
<script type="module">
import { G } from "./src/state.js"; import { update, startLevel } from "./src/game.js"; import { render } from "./src/render.js"; import { hud } from "./src/hud.js";
import { enterCave } from "./src/caves.js"; import { LEVELS } from "./src/levels/index.js";
const q = new URLSearchParams(location.search), shot = q.get("cave");
const k = (t, c) => dispatchEvent(new KeyboardEvent(t, { code: c }));
setTimeout(() => {
  document.getElementById("start").click(); k("keydown", "Space"); k("keydown", "ArrowRight");
  const out = [];
  for (let w = 0; w < 4; w++) for (const c of LEVELS[w].caves) {
    G.level = w; G.state = "play"; G.lives = 9; startLevel(); enterCave(c);
    let err = null, exited = false;
    try { for (let i = 0; i < 1800 && !exited; i++) { update(1 / 60); render(); hud(); exited = G.scene === "surface"; } } catch (e) { err = e.stack; }
    out.push({ id: c.id, err, exited, loot: G.loot.length });
  }
  if (shot) {
    const w = LEVELS.findIndex(l => l.caves.some(c => c.id === shot));
    G.level = w; G.state = "play"; startLevel(); enterCave(LEVELS[w].caves.find(c => c.id === shot));
    for (let i = 0; i < 60; i++) update(1 / 60);
    Object.assign(G.P, { x: +(q.get("x") || 400), inv: 0 }); G.camX = Math.max(0, G.P.x - 480); G.sat = .6; render(); G.paused = true; G.state = "menu";
  }
  document.title = JSON.stringify(out);
}, 300);
</script>
```

Run: starta `npx vite --port 5199 --strictPort` i bakgrunden; kör headless Edge (`--dump-dom`, ny `--user-data-dir`, `--window-size=1280,640`, `--virtual-time-budget=10000`) mot `http://localhost:5199/smoke.html` och läs `<title>`.
Expected: för alla nio grottor `err: null` och `exited: true` (spinnläge med höger nedtryckt tar sig till ljusstrålen inom 30 s).

Ta skärmbilder med `?cave=m1&x=600` (skattkammare med svamp), `?cave=m2&x=500` (mörkergrotta) och en av ytan med klippan (`?cave=` utelämnat räcker inte – använd i stället världsvyn: lägg till en tredje URL-variant i skriptet om det behövs, eller kontrollera klippan visuellt i Dusk Valley med `npm run dev`). Granska visuellt, ta bort `smoke.html` och bilderna, stoppa servern.

- [ ] **Step 6: Manuell kontroll (lämnas till användaren)**

Med `npm run dev`: hitta det synliga hålet i varje värld; skjut bort busken (Meadows x 3000, Frost x 3200); knäck stenen (Dusk x 2000, Ember x 500); flyg in i klippöppningen med Antigrav (Dusk x 4450); plocka stjärnor/pärlor/extraliv; ta dig ut genom ljusstrålen och kontrollera att ytan är som innan och att ingången är stängd; se "Secrets found" när världen är klar.

- [ ] **Step 7: Uppdatera `CLAUDE.md`**

Efter raden som börjar med `- Världar som JSON i src/levels/` lägg till:

```markdown
- Grottor (src/caves.js): per värld ett synligt hål + 1–2 dolda ingångar (bush, rock, cliff); treasure/dark; G.scene 'surface'|'cave', ytan sparas och återställs; utgång via ljusstråle
```

- [ ] **Step 8: Uppdatera specen**

I `docs/superpowers/specs/2026-09-25-world-variation-design.md`, avsnittet "### Hemligheter": ersätt stycket med

`` `G.cavesUsed` (Set med grott-id) fylls när man går in i en grotta – synlig eller dold. Vid klar värld visas "Secrets found: X/Y" där X = antal besökta grottor och Y = antal grottor i världen. Räknas per spelomgång och värld, sparas inte. ``

och i avsnittet "### Grottdata", lägg till efter listan:

`- \`loot\`-typer: \`gem\` (grön pärla, flyttar kraftmarkören), \`star\` (+200 poäng), \`life\` (extraliv, högst ett per värld). \`foes\`: \`[{ x, y }]\` – färglösa svävande fiender i mörkergrottor.`
`- Ingångsmått: hål bredd 70; buske 50×34 (ej solid, 1 träff); sten 64×46 (3 träffar); klippa 40×300 (solid, öppning ±26 kring \`entrance.y\`). Ingångar ligger minst 300 enheter från x = 0.`

- [ ] **Step 9: Commit**

```bash
git add src CLAUDE.md docs/superpowers/specs/2026-09-25-world-variation-design.md
git commit -m "Render caves, entrances, loot and darkness; show secrets found"
```
